---
mentor_node: hard_limits
layer: governance
hard_limits:
  - { id: hard_limit_1, rule: "<an absolute, never-do prohibition>" }      # replace
  - { id: hard_limit_2, rule: "<another absolute, never-do prohibition>" }
---

# Hard Limits

Bright lines — never permitted, regardless of tier, weight, or context. Keep this list short and
absolute. Each limit should be backed by an engine enforcement mechanism, not just stated.

- **hard_limit_1** — `<an absolute, never-do prohibition>`.
- **hard_limit_2** — `<another absolute, never-do prohibition>`.
