---
mentor_node: pattern
layer: learning
id: pattern_1
detects: "<the recurring issue or bottleneck to watch for>"   # replace
watches: [object_type_1]            # object types whose history reveals it — each must resolve
suggests: "<the improvement to propose when it recurs>"       # free text, e.g. update an SOP
---

# Pattern 1

`<What this pattern is and why it matters.>` Detected from the history of the object types it
watches; when it recurs, the system proposes the improvement above rather than handling it again.

| field | value |
| --- | --- |
| watches | `object_type_1` |
| suggests | `<improvement>` |
