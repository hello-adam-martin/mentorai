---
mentor_node: agent
layer: domain
id: agent_1
domain: operations
purpose: "<what this agent does>"          # replace
allowed_actions: [action_1]                # subset of Governance actions — each must resolve
objects: [object_type_2]                   # object types it reads/writes — each must resolve
---

# Agent 1

`<What this agent is responsible for.>` Operates within Governance: it may only perform the actions
listed below, at the tiers Governance assigns them.

| field | value |
| --- | --- |
| domain | `operations` |
| allowed actions | `action_1` |
| objects | `object_type_2` |
