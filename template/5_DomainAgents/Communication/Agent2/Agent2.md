---
mentor_node: agent
layer: domain
id: agent_2
domain: communication
purpose: "<what this agent does>"          # replace
allowed_actions: [action_2]                # subset of Governance actions — each must resolve
objects: [object_type_1]                   # object types it reads/writes — each must resolve
---

# Agent 2

`<What this agent is responsible for.>` Handles communication within Governance's rules.

| field | value |
| --- | --- |
| domain | `communication` |
| allowed actions | `action_2` |
| objects | `object_type_1` |
