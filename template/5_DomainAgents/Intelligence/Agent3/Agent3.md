---
mentor_node: agent
layer: domain
id: agent_3
domain: intelligence
purpose: "<what this agent does>"          # replace
allowed_actions: [action_3]                # subset of Governance actions — each must resolve
objects: [object_type_1, object_type_2]    # object types it reads/writes — each must resolve
---

# Agent 3

`<What this agent is responsible for.>` Analyses across object types within Governance's rules.

| field | value |
| --- | --- |
| domain | `intelligence` |
| allowed actions | `action_3` |
| objects | `object_type_1`, `object_type_2` |
